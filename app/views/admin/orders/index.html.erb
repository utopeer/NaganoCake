<h1>注文履歴一覧</h1>

	  <table class="table">
	  <tr>
        <th>購入日時</th>
        <th>購入者</th>

        <th>注文ステータス</th>
      </tr>
      <% @order.each do |order| %>
      <tr>
        <td><%= order.created_at %></td>
        <td><%= order.member.last_name %></td>

        <td><%= order.order_status %></td>
      </tr>
    <% end %>
    </table>
    <%= paginate @order %>

      